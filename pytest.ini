[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    golden: validates computed values against manually verified reference values
    determinism: asserts identical output across repeated runs (PRD NF4)
    layering: asserts the engine/agent layer boundary (PRD NF15)
